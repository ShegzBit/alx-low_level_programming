#include "main.h";

char *argstostr(int ac, char **av)
{
    /*iterate through strings sum up the length of each string*/
    /* allocate memory of that size to a new string*/
    /*move through each changing index for the beginning of each string to zero while less that size of alpha string*/
}