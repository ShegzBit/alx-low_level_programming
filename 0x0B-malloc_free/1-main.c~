/**
 * main - check the code for ALX School students.
 *
 * Return: Always 0.
 */
int main(void)
{
    char *s;

    s = _strdup(ALX SE);
    if (s == NULL)
    {
        printf(failed to allocate memoryn);
        return (1);
    }
    printf(%sn, s);
    free(s);
    return (0);
}
