#include <stdarg.h>
#include "variadic_functions.h"

int sum_them_all(const unsigned int n, ...)
{
  int i, sum = 0;
  va_list ap;
  if (n == 0)
    return (n);
  va_start(ap, n);
  for (i = 0; i < n; i++)
    {
      sum += va_arg(ap, int);
    }
  va_end(ap);
  return (sum);
}
