#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s : string to be counted
 * Return: length of string
 */
int _strlen(char *s)
{
int i = 0;
while (s[i] != NULL) {
i++;
}
return i;
}
