#include <stdio.h>

/**
 * print_rev - prints reverse of a string
 * @s : string to reverse
 */
void print_rev(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{

}
while (i >= 0)
{
i--;
putchar(s[i]);
}

}
