#include <stdio.h>

int main() {
    unsigned long int max_value = (unsigned long int)-1; // Calculate the maximum value
    printf("Size of unsigned long int: %lu bytes\n", sizeof(unsigned long int));
    printf("Maximum value of unsigned long int: %lu\n", max_value);

    return 0;
}
