#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the number of var passed to it
 * @argc : argument count
 * @argv : array of arguments
 * Return: 0 on success
 */

int main(int argc, char **argv)
{
(void)argv;
printf("%d\n", argc);
return (0);
}
